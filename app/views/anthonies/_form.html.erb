<%= form_for(@anthony) do |f| %>
  <% if @anthony.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@anthony.errors.count, "error") %> prohibited this anthony from being saved:</h2>

      <ul>
      <% @anthony.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
